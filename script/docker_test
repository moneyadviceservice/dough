#!/bin/bash
# Useful for testing in docker locally
docker build -t dough .
docker run \
    --interactive \
    --tty \
    --rm \
    -v "$PWD:/var/tmp/app" \
    -w /var/tmp/app \
    dough \
        ./script/test

if [ $? = 0 ]; then
    echo "All required tests passed."
else
    echo "Test(s) failed."
fi
